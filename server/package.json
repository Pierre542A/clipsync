{
  "name": "clipsync-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ClipSync relay server — presence WebSocket + relais presse-papiers + stockage temporaire des images",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "@fastify/websocket": "^11.0.1",
    "fastify": "^5.2.0"
  }
}
